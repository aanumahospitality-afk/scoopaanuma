{
  "buildCommand": "cd scooproom && npm install && npm run build",
  "outputDirectory": "scooproom/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
